Make sure matplotlib.pyplot, numpy, and PIL are all installed as packages.
Make sure the file imagerec.py is in the same directory as the Data folder that was provided to us on google drive.
Feed it the filepath to an image as the only command line argument.